'use strict';

describe('knowItAll.spells module', function() {

  beforeEach(module('knowItAll.spells'));

  describe('spells controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var spellsCtrl = $controller('SpellsCtrl');
      expect(spellsCtrl).toBeDefined();
    }));

  });
});
